# ------------------------------------------------------------------
# Monizen AI — local environment variables
# ------------------------------------------------------------------
# This file is git-ignored (.env* rule in .gitignore), so it is
# NEVER pushed to the repository. Keep your secrets here only.
#
# For production (e.g. Vercel / Netlify), do NOT commit these values.
# Instead, add them as environment variables in the hosting platform's
# settings (Vercel -> Project -> Settings -> Environment Variables).
# ------------------------------------------------------------------

# Gmail SMTP credentials — used by pages/api/sendEmail.js
# (These are read via process.env.SMTP_USER / process.env.SMTP_PASS)
SMTP_USER=abhishakejutur@gmail.com
SMTP_PASS=xiak bkhd yllz iwud
